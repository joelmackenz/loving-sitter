import Grid from '@material-ui/core/Grid';
import Typography from '@material-ui/core/Typography';

export default function Messages(): JSX.Element {
  return (
    <Grid>
      <Typography variant="h3">Messages Page</Typography>
    </Grid>
  );
}
