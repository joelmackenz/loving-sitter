import Grid from '@material-ui/core/Grid';
import Typography from '@material-ui/core/Typography';

export default function Notifications(): JSX.Element {
  return (
    <Grid>
      <Typography variant="h3">Notifications Page</Typography>
    </Grid>
  );
}
