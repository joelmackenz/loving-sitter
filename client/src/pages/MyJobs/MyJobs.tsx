import Grid from '@material-ui/core/Grid';
import Typography from '@material-ui/core/Typography';

export default function MyJobs(): JSX.Element {
  return (
    <Grid>
      <Typography variant="h3">My Jobs Page</Typography>
    </Grid>
  );
}
